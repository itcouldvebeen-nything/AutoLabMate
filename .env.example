# Environment configuration for AutoLabMate

# Run in mock mode (no API keys needed)
MOCK=false
DISABLE_API_INTEGRATIONS=true

# Backend settings
PORT=8000
HOST=0.0.0.0

# Frontend settings
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Optional API keys (only needed if using real APIs)
# OPENAI_API_KEY=sk-xxxx
# PINECONE_API_KEY=xxxx
# DATABASE_URL=sqlite:///./data.db

    